# Does attempting to (quietly) force `x` raise an error?

Does not silence any direct output to the message stream / stderr() not
from messages, warnings, or errors.

## Usage

``` r
forcing_raises_error(x)
```

## Arguments

- x:

  argument to [`force`](https://rdrr.io/r/base/force.html)

## Value

Boolean
