# Get the latest snapshot from an `epi_archive` object.

The latest snapshot is the snapshot of the last known version.

## Usage

``` r
epix_as_of_current(x)
```

## Arguments

- x:

  An `epi_archive` object

## Value

The latest snapshot from an `epi_archive` object
