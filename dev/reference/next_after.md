# Get the next possible value greater than `x` of the same type

Get the next possible value greater than `x` of the same type

## Usage

``` r
next_after(x)
```

## Arguments

- x:

  the starting "value"(s)

## Value

same class, typeof, and length as `x`
