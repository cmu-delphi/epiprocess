# versions_standardize works

    `1` did not look like (something that could be automatically converted to) a vector of versions, nor a version spacing specification, nor `NULL`.
    Caused by error in `versions_standardize()`:
    ! Cannot cast `versions`, which `is.numeric`, to <yearmonth>
    i Class of `versions` was `"numeric"`
    i (There may be a `vec_cast` method for this conversion, but we have disabled it.)

