% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive_duck.R
\name{as_duckdb_epi_archive_from_duck}
\alias{as_duckdb_epi_archive_from_duck}
\title{Construct a duckdb-backed \code{epi_archive} from a duckplyr table}
\usage{
as_duckdb_epi_archive_from_duck(
  x,
  ...,
  geo_type = NULL,
  time_type = NULL,
  other_keys = character(),
  clobberable_versions_start = NA,
  versions_end = NULL,
  validate = TRUE
)
}
\arguments{
\item{x}{A duckplyr data frame, or a dplyr lazy table/data frame that
\code{\link[duckplyr:as_duckdb_tibble]{duckplyr::as_duckdb_tibble()}} can convert.}

\item{...}{Passed to \code{\link[duckplyr:as_duckdb_tibble]{duckplyr::as_duckdb_tibble()}} when \code{x} is not already
a duckplyr data frame.}

\item{geo_type, time_type}{Optional scalar metadata. If \code{NULL}, guessed from
\code{geo_value} and \code{time_value}, respectively.}

\item{other_keys}{Character vector of additional key columns.}

\item{clobberable_versions_start, versions_end}{Version-bound metadata.
\code{versions_end = NULL} defaults to the maximum observed \code{version}.}

\item{validate}{If \code{TRUE}, validate archive invariants before returning.}
}
\value{
an \code{epi_archive} with subclass \code{epi_archive_duck}
}
\description{
Wraps an existing duckplyr data frame, or an object that
\code{\link[duckplyr:as_duckdb_tibble]{duckplyr::as_duckdb_tibble()}} can convert, without first collecting the
full table through R. The input must already use canonical archive column
names (\code{geo_value}, \code{time_value}, and \code{version}) and wide signal columns;
unlike \code{\link[=as_duckdb_epi_archive]{as_duckdb_epi_archive()}}, this constructor does not perform signal
format conversion or compactification.
}
\details{
If \code{geo_type}, \code{time_type}, or \code{versions_end} are omitted, they are inferred
from the corresponding columns. Those inference steps may read only the
needed columns or aggregates; pass them explicitly and set \code{validate = FALSE}
to avoid even those checks when wrapping a trusted DuckDB relation.
}
