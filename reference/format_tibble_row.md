# Format a tibble row as chr

Format a tibble row as chr

## Usage

``` r
format_tibble_row(x, empty = "*none*")
```

## Arguments

- x:

  a tibble with a single row

## Value

`chr` with one entry per column, of form " = "
