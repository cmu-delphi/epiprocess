# Paste `chr` entries (lines) together with `"\n"` separators, trailing `"\n"`

Paste `chr` entries (lines) together with `"\n"` separators, trailing
`"\n"`

## Usage

``` r
paste_lines(lines)
```

## Arguments

- lines:

  `chr`

## Value

string
