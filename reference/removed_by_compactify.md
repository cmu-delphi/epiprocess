# get the entries that `compactify` would remove

get the entries that `compactify` would remove

## Usage

``` r
removed_by_compactify(
  updates_df,
  ukey_names,
  abs_tol,
  init_nas_are_locf = FALSE
)
```
