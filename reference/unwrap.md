# Extract singular element of a length-1 unnamed list (validated)

Inverse of `list(elt)`.

## Usage

``` r
unwrap(x)
```

## Arguments

- x:

  a length-1 list

## Value

x\[[1L](https://rdrr.io/r/base/NumericConstants.html)\], if x actually
was a length-1 list; error otherwise
