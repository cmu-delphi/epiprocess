# "Format" column/variable name for cli interpolation

Designed to give good output if interpolated with cli. Main purpose is
to add backticks around variable names when necessary.

## Usage

``` r
format_varname(x)
```

## Arguments

- x:

  string; e.g., a colname

## Value

string
