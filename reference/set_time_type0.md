# Set, without validation, the `time_type` of an `epi_*` object

Set, without validation, the `time_type` of an `epi_*` object

## Usage

``` r
set_time_type0(x, value)
```

## Arguments

- x:

  an `epi_*` object
