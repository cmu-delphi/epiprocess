# potential is_deletion columns

the full list of potential substitutions for the `is_deletion` column
name: is_deletion, is_removal, is_deleted, deletion_flag, is_void,
is_invalid, Is_Deletion, Is_Removal, Is_Deleted, Deletion_Flag, Is_Void,
Is_Invalid

## Usage

``` r
is_deletion_column_names()
```
