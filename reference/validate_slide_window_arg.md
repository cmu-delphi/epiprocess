# Validate `.before` or `.window_size` argument

Validate `.before` or `.window_size` argument

## Usage

``` r
validate_slide_window_arg(
  arg,
  time_type,
  lower = 1,
  allow_inf = TRUE,
  arg_name = rlang::caller_arg(arg)
)
```
